import test from 'ava'

test.todo('that 10 passages during an hour does not render more than max fee (18)')

test.todo('that 100 passages during an hour does not render more than max fee (18)')

test.todo('that 2 passages during an hour uses the most expensive fee (18)')
